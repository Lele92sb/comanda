node_modules/
*.log
.DS_Store
.wrangler/
.claude/settings.local.json

# Chiavi segrete per la prova in locale (chiave Anthropic, service role Supabase).
# NON deve mai finire in git: chi legge il repo potrebbe spendere sulla tua chiave.
.dev.vars

# Build di Vite
dist/

# Copie di lavoro degli agenti: sono repository a se, non contenuto del progetto.
.claude/worktrees/
