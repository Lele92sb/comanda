{
  "name": "comanda-chef-app",
  "version": "1.3.0",
  "private": true,
  "description": "App gestionale per chef: ricettario con food cost, brigata, turni, assistente AI.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "wrangler pages dev --port 4173"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
